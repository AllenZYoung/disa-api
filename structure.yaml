uri: 'mongodb://ds021333.mlab.com:21333/temporary-disa'
username: DISA_USERNAME
password: DISA_PASSWORD
routes:
  - /entries:
      method: GET
      collection: new_entries
      filter: ~
      subroute: _id
  - /entries/internal:
      collection: new_entries
      filter:
        - meta.stage: Internal
  - /entries/public:
      collection: new_entries
      filter:
        - meta.stage: Public
  - /entries:
      method: POST
      collection: new_entries
      prepost: '../../autoIncrementId.js'
  - /entries:
      method: PUT
      collection: new_entries
  - /options:
      method: GET
      collection: options
      filter: ~
  - /users:
      method: GET
      collection: users
      filter: ~
  - /users:
      method: POST
      collection: users
